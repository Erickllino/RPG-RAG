import gradio as gr


def create_tab():
    with gr.Tab("Session Audio"):
        gr.Markdown("## Session Audio\n> Upload a session recording → transcription → summary → indexed.")
        gr.Markdown("_Not implemented yet._")
