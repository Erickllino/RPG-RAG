[project]
name = "vllm-server"
version = "0.1.0"
description = "Local LLM server Using vLLM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "vllm >= 0.8.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
run-local = "main:main"